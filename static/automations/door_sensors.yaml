- alias: "Door: open"
  mode: single
  trigger:
    - platform: state
      entity_id: binary_sensor.front_door
      from: "off"
      to: "on"
  action:
    - service: notify.dan
      data:
        message: Door open
- alias: "Door: closed"
  mode: single
  trigger:
    - platform: state
      entity_id: binary_sensor.front_door
      from: "on"
      to: "off"
  action:
    - service: notify.dan
      data:
        message: Door closed
