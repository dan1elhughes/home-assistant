- alias: "Curtains partially closed at sunset"
  mode: single
  triggers:
    - trigger: sun
      event: sunset
  action:
    - action: cover.set_cover_position
      target:
        entity_id: cover.curtains
      data:
        position: 60

- alias: "Curtains fully closed at dawn"
  mode: single
  triggers:
    - trigger: numeric_state
      entity_id: sun.sun
      attribute: elevation
      above: -6
  condition:
    - alias: "Home"
      condition: state
      entity_id: group.presence_home
      state: home
  action:
    - alias: "Slowly close curtains"
      action: v6_quietdrift.set_switchbot_curtain_position
      data:
        position: 0
        entity_id: cover.curtains
        speed: 1
